[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Agar React Router bisa jalan di Netlify tanpa error 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
